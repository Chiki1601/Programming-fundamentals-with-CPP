//area of rectangle
#include <iostream>
#include<math.h>
using namespace std;

int main()
{
    int length,breadth;
    int area;
    cout<<"Enter the length of rectangle:";
    cin>>length;
    cout<<"Enter the length of breadth:";
    cin>>breadth;
    area =length*breadth;
    cout<<"Area of rectangle:"<<area;

    return 0;
}